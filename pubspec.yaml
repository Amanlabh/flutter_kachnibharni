name: social_media_app
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  appwrite: ^13.0.0
  provider: ^6.0.0 # State management
  font_awesome_flutter: ^10.3.0 # Icons for bottom navigation
  cupertino_icons: ^1.0.8 # For iOS style icons
  image_picker: ^0.8.4+4 # Added image_picker here
  permission_handler: ^10.2.0 #permission handeling for device
  nfc_manager: ^3.0.0  # For NFC scanning
  qr_code_scanner: ^1.0.0  # For QR code scanning



  


 
  #appwrite: ^5.0.0 # appwrite for authentication 


  

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  

flutter:
  uses-material-design: true

  # You can add your assets here if you have any.
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
